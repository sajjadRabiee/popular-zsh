name: CI

on:
  push:
  pull_request:

jobs:
  shell-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate zsh scripts
        run: |
          zsh -n popular.zsh
          zsh -n install.sh
