# Contributors

## Author

**Sajjad Rabiee** — creator and maintainer of `popular.zsh`.

## AI Contributions

**Claude (Anthropic)** — assisted with the following during development:

- Designed and implemented `pcli` (`lib/popular/cmd-cli.zsh`): the interactive sub-shell mode that lets saved command names work directly without the `p` prefix, including the `[p]` RPROMPT badge, short aliases, `command_not_found_handler`, and first-word tab completion inside the session.
- Added `pcli` to the `phelp` reference table (`lib/popular/ui.zsh`).
- Updated all documentation to cover `pcli`: `README.md`, `CONTRIBUTING.md`, `docs/launch-post.md`, and every page under `docs/wiki/` (`Command-Reference.md`, `Home.md`, `Examples.md`, `Installation.md`, `Templates.md`).
- Redesigned the project banner SVG (`assets/popular.svg`) with a richer dark-terminal aesthetic: gradient title, glowing `p` logo, command pill badges, mini embedded terminal preview.
- Created the initial GitHub release (`v1.0.0`) with full release notes.

Model: **Claude Sonnet 4.6** by [Anthropic](https://www.anthropic.com).
